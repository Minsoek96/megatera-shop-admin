import { Category } from '../../types';

type ProductNewFormProps = {
    categories: Category[]
    onComplete: () => void
}
export default function ProductNewForm({ categories, onComplete }:ProductNewFormProps) {
  return null;
}
